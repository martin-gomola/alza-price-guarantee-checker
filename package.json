{
  "name": "alza-price-guarantee-checker",
  "version": "1.2.1",
  "private": true,
  "scripts": {
    "test": "node --test",
    "build": "rm -rf dist && mkdir -p dist && zip -r dist/alza-price-guarantee-checker-v$(node -p \"require('./package.json').version\").zip manifest.json icons/ src/ PRIVACY.md"
  },
  "devDependencies": {}
}
